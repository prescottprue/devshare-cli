/* global describe, it, expect */
import library from '../../src'

describe('devshare-cli', () => {
  describe('methods', () => {
    it('has initRef', () => {
      expect(library).to.exist
    })
  })
})
